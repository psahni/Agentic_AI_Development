* Setup python env
* Setup python virtual env
  *   python3 -m venv .venv && source .venv/bin/activate
* Install libs
  * pip install langgraph langchain-core langchain_groq python-dotenv
* Setup key - I used free api key provided by Groq
* Set the set in .env
* Run - `python hello_graph.py`
